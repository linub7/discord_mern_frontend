import { styled } from '@mui/system';

const MainContainer = styled('div')({
  flexGrow: 1,
  width: '100%',
});

const FriendsList = () => {
  return <MainContainer>FriendsList</MainContainer>;
};

export default FriendsList;
